namespace Kluster.Shared.Constants;

public enum OtpRoute
{
    Email,
    Phone
}

public static class OtpRouteStrings
{
    public const string Email = nameof(Email);
    public const string Phone = nameof(Phone);
}